import Loading from './../components/Loading'

const LoadingPage = () => {      
    return (
        <div style={style}>
            <Loading />
        </div>
    )
}

const style = {
    paddingTop:300,
    fontSize:45
}

export default LoadingPage;
